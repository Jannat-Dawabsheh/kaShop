import { createTheme } from "@mui/material";

const theme=createTheme({

      palette: {
    mainColor: {
      main: '#4fc4ca',
      contrastText: '#312d5f',
    },

    darkColor: {
      main: '#312d5f',
      contrastText: '#312d5f',
    },
  },
})

export default theme